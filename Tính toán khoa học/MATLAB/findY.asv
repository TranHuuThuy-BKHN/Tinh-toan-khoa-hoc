function y = findY(L, P, b)
    n = length(b)
    y = 1:n
    
    b1 = b(p)
    
    for i = 1 : n 
        y(i) = b1(i) -
        
        y(i+1:n) = 
        for j = 1 : i - 1
            y(i) = y(i) - y(j) * L(i, j)
        y(i) = y(i) / L(i, i)
    end

end